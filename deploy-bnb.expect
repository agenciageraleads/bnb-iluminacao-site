#!/usr/bin/expect -f
set timeout 10
set host "187.77.37.213"
set user "root"
set password "ImPh,RbdVk4.gLuc@X8t"

spawn ssh -o StrictHostKeyChecking=no ${user}@${host} "ls -la /var/www /root"
expect {
    "assword:" {
        send "${password}\r"
        exp_continue
    }
    eof
}
